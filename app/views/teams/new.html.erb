<div class="container">
  <div class="p-signup__form py-3 px-4">
    <%= form_with model: @team, local: false do |f| %>
      <%= render 'teams/form', f: f %>
      <%= f.submit '作成' %>
    <% end %>
  </div>
</div>